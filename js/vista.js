/* =========================================================
   VISTA
   Reparte el trabajo de repintar la pantalla: el árbol de
   secciones y el formulario de lo que haya elegido.
   ========================================================= */

function pintarTodo(){
  pintarArbol();
  pintarZona();
}
